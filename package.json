{
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "start:dev": "tsc-watch --noClear -p ./calc --onSuccess \"node ./dist/app.js\"",
    "test": "jest --verbose --silent",
    "test:watch": "pnpm run test -- --watch"
  },
  "dependencies": {
    "@jest/globals": "^30.3.0"
  }
}